// Number 3
def funcp():
    stack = []
    b = input("Write any number of your choice")
    while b >= stack.len():
        stack.append(b)
        c = input("Do you want to add another number")
        if c == "yes":
            continue
        else:
            break
        print(stack)
    
funcp()
